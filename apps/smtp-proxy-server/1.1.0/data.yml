additionalProperties:
  formFields:
    - default: 8025
      edit: true
      envKey: PANEL_APP_PORT_SMTP
      labelZh: SMTP 端口
      labelEn: SMTP port
      label:
        en: SMTP port
        zh: SMTP 端口
      required: true
      rule: paramPort
      type: number
    - default: 11143
      edit: true
      envKey: PANEL_APP_PORT_IMAP
      labelZh: IMAP 端口
      labelEn: IMAP port
      label:
        en: IMAP port
        zh: IMAP 端口
      required: true
      rule: paramPort
      type: number
    - default: ""
      edit: true
      envKey: PROXY_URL
      labelZh: 服务地址
      labelEn: Server Address
      label:
        en: Server Address
        zh: 服务地址
      required: true
      type: text
